{
  "name": "js-course",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "mocha ./lesson1/test --recursive",
    "test-report": "mocha test --recursive --reporter mocha-junit-reporter",
    "lint": "eslint . --ext .ts,.js --max-warnings 0",
    "lint_fix": "eslint . --ext .ts,.js --fix"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "mocha": "^8.1.3",
    "mocha-junit-reporter": "^2.0.0",
    "uuid": "^8.3.2"
  },
  "dependencies": {
    "nyc": "^15.1.0"
  }
}
